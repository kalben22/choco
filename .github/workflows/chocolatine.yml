name: Chocolatine Workflow

on:
  push:
    branches-ignore:
      - 'no-ga/**'
  pull_request:
    branches-ignore:
      - 'no-ga/**'

env:
  MIRROR_SSH_URL: "git@github.com:kalben22/choco2.git"
  EXECUTABLES: "mysh"

jobs:
  check_compilation_and_coding_style:
    name: Check compilation and coding style
    runs-on: ubuntu-latest
    container: epitechcontent/epitest-docker
    if: ${{ github.repository != 'kalben22/choco2' }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Check repository for banned/unwanted files
        run: banana-check-repo

      - name: Compile project with epiclang (generate report)
        run: |
          make CC=epiclang 2>&1 | tee coding-style-reports.log
        timeout-minutes: 2

      - name: Report coding style errors as annotations
        if: always()
        run: |
          if [ ! -f coding-style-reports.log ] || [ ! -s coding-style-reports.log ]; then
            echo "No coding style errors detected"
            exit 0
          fi

          while IFS= read -r line; do
            file=$(echo "$line" | cut -d':' -f1)
            lineno=$(echo "$line" | cut -d':' -f2)
            col=$(echo "$line" | cut -d':' -f3)
            message=$(echo "$line" | cut -d':' -f4-)

            if [[ "$line" == *"[Banana]"* ]]; then
              echo "::error file=${file},line=${lineno},col=${col}::${message}"
            fi
          done < coding-style-reports.log

      - name: Fail if any coding style errors exist
        if: always()
        run: |
          if grep -q "\[Banana\]" coding-style-reports.log; then
            echo "Coding style errors detected"
            exit 1
          fi

      - name: Clean project build artifacts
        run: make clean

      - name: Check that executables exist and are executable
        run: |
          FAIL=0
          for exec in $(echo "$EXECUTABLES" | tr ',' '\n'); do
            if [ ! -f "$exec" ]; then
              echo "::error file=${exec}::Executable '${exec}' was not found"
              FAIL=1
            elif [ ! -x "$exec" ]; then
              echo "::error file=${exec}::Executable '${exec}' is not executable"
              FAIL=1
            fi
          done
          exit $FAIL

  run_tests:
    name: Run tests
    runs-on: ubuntu-latest
    container: epitechcontent/epitest-docker
    needs: check_compilation_and_coding_style
    if: ${{ github.repository != 'kalben22/choco2' }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Run unit tests
        run: make tests_run
        timeout-minutes: 2

  push_to_mirror:
    name: Push to mirror repository
    runs-on: ubuntu-latest
    needs: run_tests
    if: ${{ github.event_name == 'push' && github.repository != 'kalben22/choco2' }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Mirror repository to Epitech
        uses: pixta-dev/repository-mirroring-action@v1
        with:
          target_repo_url: ${{ env.MIRROR_SSH_URL }}
          ssh_private_key: ${{ secrets.GIT_SSH_PRIVATE_KEY }}
